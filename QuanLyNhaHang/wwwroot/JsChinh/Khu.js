document.addEventListener("DOMContentLoaded", function () {
    datas = [
        {
            className: ".cbSanh",
            placeholder: "-- Sảnh --",
            action: "Sanh",
        }
    ];
    //configCbDataBase(datas);
}); // hết hàm DOMContentLoaded
